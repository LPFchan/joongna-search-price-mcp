services:
  joongna-search-price-mcp:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      HOST: 0.0.0.0
      PORT: 8000
      JOONGNA_CACHE_TTL_SECONDS: 300
      JOONGNA_PUBLIC_BASE_URL: https://joongna.lost.plus
      ALLOWED_ORIGINS: https://*.lost.plus
